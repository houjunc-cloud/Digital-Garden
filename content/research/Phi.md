[[rank one comparison + matching#^72ed67]]

An approach to prove $\Phi: Wh_{G_X} \isom Wh_{G}$ is:

>1. $Wh_{G_X}$ is isomorphic to $Wh_{G}$ over the regular locus of 
>2. The isomorphism extends to the codimension one part

Then since both sides are normal affine, the isomorphism extends to the whole space.

First, $M \isom V_M \times ^H G$ where $V_M = V_\rho \bigoplus (\mathfrak h^\perp \cap \mathfrak g^{*,e})$

Denote by $\kappa_G$ the Kostant section of $\mathfrak g \to \mathfrak g//G = \mathfrak c_G$. And denote $\iota: \mathfrak c_X \to \mathfrak c_G$ the induced inclusion from moment map $\mu$.

Then $Wh_{G, reg} = \mu^{-1}(\kappa_G(\iota(\mathfrak c_{X,rs})))$. For $c \in \mathfrak c_{X,reg}$ , set $x_c:=\kappa_G \circ \iota(c)$. $J_{G,c} = Z_G(x_c)$. It acts on $Wh_{G, reg, c} = \mu^{-1}(x_c)$ which is induced from the $G$ - action on $M$. 